name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
    # Testing
  - codecov
  - matplotlib
  - numpy
  - pandas
  - pytest
  - pytest-cov
  - seaborn
  - scikit-learn

  # Pip-only installs
  #- pip:
  #  - codecov

