name: docs
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Package depends
  - numpy
  - matplotlib
  - pandas
  - sphinx-autoapi

    # Pip-only installs
  - pip:
    - sphinx==6.0.0
    - https://github.com/revitron/revitron-sphinx-theme/archive/master.zip
