name: docs
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Pip-only installs
  - pip:
    - sphinx-autoapi
    - sphinx==6.0.0
    - numpy
    - matplotlib
    - pandas
    - plotly
    - seaborn
    - scipy
    - modeller
    - https://github.com/revitron/revitron-sphinx-theme/archive/master.zip
