name: docs
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Package depends
  - numpy
  - matplotlib
  - pandas
  - sphinx-autoapi

    # Pip-only installs
  - pip:
    - https://github.com/revitron/revitron-sphinx-theme/archive/master.zip
